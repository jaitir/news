import { EventRegistryDashboard } from "@/components/event-registry-dashboard";

export const dynamic = "force-dynamic";
export const revalidate = 0;

export default function ResearchPage() {
  return <EventRegistryDashboard />;
}
